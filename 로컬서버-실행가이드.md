# 로컬 서버 실행 가이드

웹사이트를 정상적으로 테스트하려면 로컬 서버를 실행해야 합니다. 파일을 직접 열면(`file://` 프로토콜) CORS 정책으로 인해 JSON 데이터를 불러올 수 없습니다.

## 🚀 빠른 시작

### 방법 1: Python 사용 (권장)

Python이 설치되어 있다면 가장 간단합니다.

```bash
# 프로젝트 폴더로 이동
cd "C:\Users\win\Desktop\밝은내일 인테리어샵"

# Python 3.x
python -m http.server 8000

# 또는 Python 2.x
python -m SimpleHTTPServer 8000
```

브라우저에서 `http://localhost:8000` 접속

### 방법 2: Node.js 사용

Node.js가 설치되어 있다면:

```bash
# 프로젝트 폴더로 이동
cd "C:\Users\win\Desktop\밝은내일 인테리어샵"

# http-server 설치 (최초 1회만)
npm install -g http-server

# 서버 실행
http-server -p 8000
```

또는 npx 사용 (설치 불필요):

```bash
npx http-server -p 8000
```

브라우저에서 `http://localhost:8000` 접속

### 방법 3: PHP 사용

PHP가 설치되어 있다면:

```bash
# 프로젝트 폴더로 이동
cd "C:\Users\win\Desktop\밝은내일 인테리어샵"

# 서버 실행
php -S localhost:8000
```

브라우저에서 `http://localhost:8000` 접속

### 방법 4: VS Code Live Server 확장

1. VS Code에서 프로젝트 폴더 열기
2. "Live Server" 확장 설치
3. `index.html` 파일 우클릭 → "Open with Live Server"

## 📝 주의사항

- **포트 번호**: 8000번 포트가 사용 중이면 다른 포트 사용 가능 (예: 8080, 3000)
- **방화벽**: Windows 방화벽에서 허용 요청이 나올 수 있습니다. "허용"을 선택하세요.
- **종료**: 터미널에서 `Ctrl + C`로 서버 종료

## ✅ 확인 방법

서버가 정상적으로 실행되면:
- 브라우저 주소창에 `http://localhost:8000` 또는 `http://127.0.0.1:8000` 표시
- 포트폴리오 페이지에서 CORS 오류가 사라짐
- 제품 페이지에서 데이터가 정상적으로 로드됨

## 🔧 문제 해결

### "포트가 이미 사용 중입니다" 오류
다른 포트 번호 사용:
```bash
python -m http.server 8080
# 또는
npx http-server -p 8080
```

### Python이 설치되어 있지 않음
- [Python 공식 사이트](https://www.python.org/downloads/)에서 다운로드
- 또는 Node.js 방법 사용

### Node.js가 설치되어 있지 않음
- [Node.js 공식 사이트](https://nodejs.org/)에서 다운로드
- 또는 Python 방법 사용

---

**팁**: 개발 중에는 서버를 계속 실행해두고, 파일을 수정하면 브라우저에서 새로고침(F5)만 하면 됩니다.

